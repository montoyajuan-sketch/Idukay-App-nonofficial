node_modules/
dist/
downloads/
*.log
.DS_Store
Thumbs.db

# Notas de release opcionales: por defecto NO se suben (así el workflow usa
# notas automáticas). Solo se incluyen si las agregas a mano con:
#   git add -f RELEASE_NOTES.md
# antes de crear el tag de esa versión.
RELEASE_NOTES.md
